# Sources

Place only source files you are permitted to use in this directory. Git ignores
the files by default so a normal commit does not publish them.

Before relying on a claim, open the original source yourself and record its
page or section in `../source-ledger.md`.
